import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login Page",
  description: "Login Page",
};

const Login = () => {
  return (
    <div>
      <p>Login Page</p>
    </div>
  );
};

export default Login;
